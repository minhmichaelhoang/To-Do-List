# Optional. Gesetzt -> Server nutzt Postgres (PostgreTaskRepository/PostgreProjectRepository,
# Schema siehe db/schema.sql). Ungesetzt -> In-Memory-Adapter mit fester Seed-Daten (Default fuer lokale Entwicklung).
# DATABASE_URL=postgres://user:password@localhost:5432/to_do_list

# Optional, Default 3000.
# PORT=3000
