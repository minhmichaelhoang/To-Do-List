import { Button } from "@/components/Button.tsx"

interface Checkbox {
	onBoxChecked: () => void;
}

export function Checkbox(taskId: string) {
	return (
		<Button onClick={() => }>

		</Button>
	)
}
