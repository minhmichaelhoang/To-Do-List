{
	"name": "practice-multithreading",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"start": "npm run start --workspace=backend",
		"start:server": "npm run start:server --workspace=backend",
		"dev": "npm run dev --workspace=frontend"
	},
	"workspaces": [
		"backend",
		"frontend",
		"shared"
	],
	"devDependencies": {
		"prettier": "^3.9.6"
	}
}
